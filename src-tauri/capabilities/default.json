{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permessi della finestra principale: SQL per l'archivio, Bluetooth per scaricare dai computer subacquei, apertura di un indirizzo nel browser di sistema per l'accesso, e il ritorno da quell'accesso su iOS. Nessuna shell e nessun filesystem arbitrario.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "sql:default",
    "sql:allow-load",
    "sql:allow-execute",
    "sql:allow-select",
    "sql:allow-close",
    "blec:default",
    "opener:default",
    "opener:allow-open-url"
  ]
}
